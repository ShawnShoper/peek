# PEEK 本地效率工具产品需求文档

文档版本：0.3.0（当前应用构建版本 0.3.0，Build 3）
平台：macOS 13.0 及以上
形态：原生菜单栏应用 + 文件查找浮窗 + 设置窗口

## 1. 产品目标

为 macOS 用户提供一个轻量、本地优先的效率工具：通过全局文件查找快速定位应用、文件与文件夹，同时完成截图、标注、马赛克、钉图、滚动拼接、OCR 与按需保存。

## 2. 功能范围

### P0：应用入口与单实例

- 常驻菜单栏，提供文件查找、区域截图、滚动截图、OCR 截图、设置与退出入口；启动时不得自动打开普通主窗口。
- 全局只允许一个 PEEK 实例；重复启动时激活已有实例，不产生第二个常驻进程。
- 注册系统级快捷键：
  - `Control + Space`：文件查找；
  - `Option + Command + 2`：区域截图；
  - `Option + Command + 3`：滚动截图；
  - `Option + Command + 4`：OCR 截图。
- 快捷键在其他应用获得焦点时仍可触发；注册失败时显示可读原因。
- 四项快捷键均可在设置页录入、停用和恢复默认，并在应用重启后保持；菜单栏必须同步显示当前组合。
- 应用完整支持简体中文和 English；默认跟随 macOS 系统语言，也允许用户在设置中手动指定。语言切换后应明确提示重新打开应用，以统一 AppKit 菜单、系统提示窗与 SwiftUI 页面。
- 保存前检查 PEEK 菜单占用、四项配置内部重复和 macOS Symbolic Hot Keys，并以 Carbon 独占注册作为最终竞态仲裁。`Control + Space` 被输入法占用时必须显示冲突并允许改键。
- 系统快捷键列表不可读取时必须显示“检查受限”；不得宣称能枚举所有非 Carbon 第三方快捷键。
- 截图进行中允许取消当前任务，并阻止重复启动另一条截图流程。

### P0：文件查找

- `Control + Space` 打开或关闭居中的查找浮窗；快捷键冲突时仍可从菜单栏进入，并在设置中自定义组合。
- 查询范围包含应用程序、普通文件和文件夹，并包含隐藏文件与隐藏文件夹；受 macOS 隐私权限保护且无法读取的位置应统计为不可访问，不得伪造结果。
- 不依赖 Spotlight；查询只能读取 SQLite 中最近一次完整提交的索引代，打开浮窗、空查询、输入字符和切换分类均不得触发文件系统扫描。
- App Sandbox 仅允许索引应用目录中的 `.app` 应用包和用户通过 `NSOpenPanel` 明确授权的目录；应用根不得写入普通系统文件或文件夹。目录授权使用 app-scoped Security-Scoped Bookmark 跨重启恢复，移除授权后该目录结果必须立即停止展示。
- 应用启动约 3 秒后尝试首次后台索引；先按应用目录、再按用户授权文件目录执行有界快速通道，并按真实扫描吞吐展示预计剩余时间。每个目录完成原子提交后立即可查，不得等待所有目录完成，也不得在查询时补扫。首次快速档目标为单核不超过 5%、256 项微批次和每秒约 2,500 个元数据项；后续增量恢复单核 0.6%、16–32 项微批次和每秒约 160 个元数据项且不展示 ETA。设置页不得提供全局“立即索引”，而应对每个配置目录提供独立刷新入口；大目录直接展示按钮，小目录和应用目录在行菜单中提供。同一时间只允许刷新一个目录，其余请求排队。正常调度周期为 30 分钟并允许系统最多 15 分钟容差，每 7 天执行一次完整重建。未完成索引的文件允许暂时查不到。
- 每次增量维护扫描所有 dirty 根，并额外轮换一个最久未成功更新的 clean 根，用于逐步发现 Finder、浏览器和下载器等外部进程产生的变化；不得把每轮增量退化成所有授权根的全量扫描。
- 文件查找每次打开默认选择“全部”。“全部”结果统一按匹配度排序，不按类型强制分组；仅标准化显示名称相同时按“应用程序 > 文件 > 文件夹”处理。
- 所有已索引 `.app` 必须统一读取 macOS 本地化资源中的当前语言、简体中文、繁体中文和英文名称，同时保留 Bundle 名与磁盘包名作为别名；支持中文名称、中文全拼、拼音首字母、英文单词首字母、大小写/音调无关以及非连续字符模糊匹配。
- 顶部支持“全部、应用、文件、文件夹”分类，键盘支持上下选择、`Enter` 打开、`Tab` 切换分类、`Esc` 关闭；不得吞掉 VoiceOver 或带修饰键的系统组合。
- 左侧展示图标、名称与路径；右侧使用 Quick Look 展示预览、完整路径、类型、大小、时间、应用版本与隐藏状态。
- 底部功能区按顺序展示打开、复制、在访达显示、复制路径、移动到、复制到、隔空投送。
- 移动和复制不得静默覆盖目标，系统/只读项目不得移动，目录不得复制或移动到自身子目录。
- 已提交、可查询的 active 索引设置 30 万项硬上限，并跳过缓存、容器数据、构建产物和版本库对象目录。为原子切换保留的 staging/旧代允许短时占用额外数据库行，但必须不可查询并由低占用维护分批清理。
- 后续自动增量的后台枚举、元数据提取、SQLite 写入和旧代清理都必须经过同一低占用预算：目标单核 0.6%，以连续 60 秒平均值低于 1% 验收。键盘和鼠标活动不得暂停索引，避免持续使用电脑时索引长期饥饿；查找面板、截图、OCR、文件复制/移动、低电量和严重温度压力期间仍须暂停。首次和用户主动目录刷新使用不超过单核 5% 的目标加快完成，并遵守相同的 PEEK 交互任务与系统压力暂停边界。
- 每个授权根使用 staging generation；只有完整成功才原子切换 active generation，取消、权限失效或中途错误必须保留旧的可查询代。

### P0：区域截图

- 截图开始前冻结所有显示器画面，再显示不污染结果的多屏选区遮罩。
- 未建立选区时显示当前像素的放大预览、HEX 与 RGB；单击复制色值，鼠标移动达到 `4pt` 后才进入拖选，不得因 `mouseDown` 立即建框而与取色冲突。
- 支持自由拖选、双击窗口选取、移动选区、八方向缩放和方向键微调。
- 冻结画面上必须覆盖一层视觉透明但拦截全部鼠标事件的全屏蒙层；底层应用不得收到点击、拖拽或文字选择事件。选框只记录同一份冻结桌面上的几何范围，不是独立截图窗口。
- 鼠标松开后立即在选区底部展示完整横向工具栏。默认工具为移动选区，用户可直接拖动整个选框；移动过程中实时采样同一份冻结画面的对应区域，不得重新抓取实时桌面。
- 点击标注、马赛克、缩放、OCR、二维码、钉图、复制、保存或完成时，始终使用会话开始时的同一份冻结图像；底部空间不足时工具栏移动到选区上方并保持在当前显示器内。“完成”写入剪贴板并结束截图。
- 选区展示尺寸反馈，并避免生成空白或越界图像。
- 截图会话可取消；异常或长时间无响应时给出可操作的错误提示。

### P0：截图编辑与马赛克

- 支持选择与移动已有标注。
- 支持矩形、椭圆、箭头、直线、画笔、荧光笔、文字和序号。
- 支持像素化马赛克，不以半透明涂层替代真实像素处理。
- 支持撤销、重做、复制、另存、完成和取消。
- 支持从选区内联工具栏直接复制、保存、钉图、OCR 或二维码识别。
- 支持工具栏放大/缩小、触控板捏合、双指平移及 `⌘+`、`⌘-`、`⌘0`；缩放只改变编辑视图，标注继续使用原图坐标，导出保持原始像素尺寸。

### P0：剪贴板与按需保存

- 普通区域截图的双击、`Enter` 与“完成”以及滚动截图编辑完成后，默认只复制最终结果到系统剪贴板，不创建历史截图文件。
- 只有用户点击选区或编辑器工具栏中的“保存”时才打开系统保存面板并写入 PNG；应用不维护截图历史主页面。
- 保存失败、路径不可用或剪贴板写入失败时显示明确错误，不丢失当前编辑结果。

### P0：钉图

- 支持同时存在多张钉图。
- 钉图窗口置顶、无边框，可拖动、缩放、旋转和调节透明度。
- 支持复制、另存和单独关闭；关闭一张不影响其他钉图。

### P0：OCR

- 使用 macOS Vision 在本机识别简体中文、繁体中文和英文。
- 支持从专用 OCR 截图和编辑器发起识别。
- OCR 结果按阅读顺序展示文本与置信度。
- 结果支持编辑、复制全部和保存为 TXT。
- 无文字或识别失败时显示明确提示。
- 图片和识别结果不得上传网络。

### P0：二维码识别

- 支持从选区内联工具栏和长图编辑器发起本地二维码识别。
- 选区形成后可在后台进行低优先级、本地二维码存在性检测；检测成功时只在选区工具栏下方显示指向二维码按钮的提示，不得自动打开结果。用户主动点击二维码按钮后才执行完整识别和结果展示。
- macOS 菜单栏下拉菜单不得新增二维码入口、检测状态或提示文案。
- 纯文本结果支持编辑和复制；HTTP/HTTPS 网址支持复制，并且只在用户点击后交给默认浏览器打开。
- 二维码内嵌图片数据或 `data:image` 内容直接在 OCR 风格双栏结果窗口中预览，并支持复制和保存。
- 同一选区存在多个二维码时支持逐项切换，并在原图中标出当前结果位置。
- 不自动请求远程图片地址，不自动跳转网址；无二维码或内容不受支持时显示明确提示。

### P0：滚动截图

- 触发时对鼠标位置执行 AX hit-test，沿元素父链优先选择最近的 `AXScrollArea` / `AXWebArea`；鼠标未命中滚动容器但仍在当前目标 App 窗口内时预选整窗，但不得把整窗标记为已验证滚动区。
- 选区遮罩显示期间，只允许在触发时直接 hit-test 验证的单一 AX 滚动区域与目标整窗之间切换；不得把整棵 AX 树压平成矩形后按面积猜选，也不得在每次 mouseMoved 时同步查询 AX。用户开始拖动、移动、缩放或键盘微调后立即停止自动跟随，并以自定义选区为最终结果。
- 自动识别结果只作为待确认选区；用户按 `Enter` 确认，也可拖动修正。修改后清除 AX 语义身份，但必须按最终选区重新绑定精确 `PID + windowID + windowFrame`，不得因此静默降级为手动滚动。跨屏窗口或滚动区预选面积最大的单屏部分并明确提示。
- 用户确认滚动容器的可见区域后，采集期间固定相同屏幕坐标。
- 手动模式由用户滚动目标内容，通过浮层停止并拼接或取消。
- 自动模式使用公开的 Post Event 权限发送标准系统滚轮事件；每次投递前复核目标窗口身份、位置、前台状态、选区覆盖率及滚动点遮挡。
- App Sandbox 构建不得依赖跨应用 AX 层级一定可读；AX hit-test 受限时必须退回整窗预选并保留自由框选，不能使用私有接口或临时沙盒例外绕过。非沙盒直接分发版本才可把完整 AX 自动框选作为独立能力评估。
- 未获得 Post Event 权限或无法稳定绑定单一窗口时不得执行自动滚动，必须明确提示原因，不得静默切换模式；用户可在设置中主动选择手动模式。
- 自动模式的常规实际步长为选区高度的 35%，同时受用户配置的像素上限约束，并以选区高度的 45% 作为硬上限；不得通过固定 100 像素下限放大小选区的滚动比例。
- 首次试滚连续没有画面变化时明确停止，不得把首帧作为成功长图；已经产生可靠滚动后再出现重复帧，才视为到达底部。
- 相邻帧通过像素差异估算重叠，过滤重复帧并纵向拼接。
- 与选区边缘连续相连且边界稳定的固定标题栏/侧栏应在拼接前裁除，并对剩余正文重新估算重叠。
- 低纹理、内部悬浮层、滚动边界漂移或重叠匹配不可靠时停止并提示重新框选；自动和手动模式均禁止使用固定比例强制生成可能错版的长图。
- 重叠识别、滚动区域、目标未滚动或目标窗口变化导致停止时，提供减小步长并重新框选、重新框选、切换手动、复制诊断与取消。失败前已有至少 2 帧通过可靠性校验时，额外提供编辑并保存已完成部分；该结果必须明确标记为失败前的可靠前缀，第 2 帧首次匹配失败时不得提供。重新采集必须等上一轮完整结束后再进入，不得递归撞入正在采集状态。
- 恢复提示必须说明新一轮从目标当前画面开始，用户应先把页面回到本次开始位置或起点；复制的诊断仅包含错误、模式、选区、实际步长与窗口标识等文字，不保存截图。
- 提供最大帧数、最长时长、输出高度和总像素限制，防止无界采集。

### P0：设置页面

- 参考 HapiGo 的原生 macOS 设置结构，在窗口顶部提供“通用、搜索、快捷键、截图、外观、关于”六个入口；不照搬其无关业务功能。
- 通用页配置登录时打开、菜单栏运行方式、查找窗口所在屏幕、位置恢复策略和应用语言，并可恢复非破坏性默认设置。
- 搜索页左侧只保留“文档搜索、应用搜索、搜索排除”三个入口，避免向普通用户暴露索引实现细节。文档搜索使用“目录、权限、索引状态”三列表格，选中行后在右侧显示目录图标、路径、权限、索引状态、范围与可用操作。默认按固定顺序列出登录账户真实 Home 下的文稿、桌面和下载，不展示或索引 App Sandbox 容器 Home；未被已有授权覆盖时，行内和详情区直接显示“授权”按钮，不使用警告图标。默认项可逐项删除，“恢复默认”必须恢复完整三项。相同目录的系统本地化名称与磁盘英文名称必须合并为一行。应用搜索提供一组可逐项删除、可一键恢复的默认目录，并允许添加自定义应用目录；搜索排除初始为空，只展示用户自行添加的文件或目录，不提供预置可见排除项。索引期间侧栏显示“已索引 / 已发现”动态计数和进度条，发现总数允许增长；根目录在扫描期间发生变化时安排补充增量。
- 首次启动时，如果当前启用的默认文档目录仍有未被授权覆盖的项目，只显示一次轻量提示，并提供“打开设置”和“稍后”；不得自动连开多个目录选择器。用户选择“打开设置”后必须直接进入“搜索 > 文档搜索”，以后仍可随时通过每行“授权”或“重新授权”按钮完成配置。
- 快捷键页统一修改文件查找及三个截图快捷键，支持 `Delete` 停用、`Esc` 取消录入和一键恢复默认；同时展示注册状态、系统冲突、检查受限或失败原因。
- 截图页展示当前截图就绪状态，区分可用、忙碌、缺少屏幕录制权限、首次授权后需重启等状态；展示屏幕录制与自动滚动 Post Event 权限并提供系统设置入口。
- 截图页配置当前窗口滚动区识别、自动/手动采集、滚动距离和最大帧数；明确双击、Enter 或完成后直接写入剪贴板，不维护截图历史。
- 外观页配置系统/浅色/深色、搜索结果密度和预览栏显示；关于页展示版本、本地处理与授权边界及脱敏诊断复制。
- 所有设置即时持久化，不提供总“保存”按钮；语言支持“跟随系统、简体中文、English”，切换后明确提示重新打开应用。

## 3. 核心流程

### 文件查找

1. 用户按 `Control + Space` 或从菜单栏打开查找浮窗；冲突时使用菜单入口或先在设置中改键。
2. 应用仅查询 SQLite 已提交索引并立即结束本次查询；后台索引调度与本次查询完全解耦。
3. 用户输入中文、拼音或英文模糊词，或用 `Tab` 切换分类；结果持续更新但保持当前有效选择。
4. 用户查看 Quick Look 和元数据，用 `Enter` 打开，或从底部功能区执行明确可用的本地文件操作。

### 首次截图与授权

1. 用户从菜单栏或快捷键发起截图。
2. 应用请求 macOS 屏幕录制权限。
3. 用户授权后，应用明确提示完全退出并重新启动。
4. 重启后再次发起截图，进入多屏选区。
5. 若仍未授权，展示失败原因和系统设置入口，不反复静默请求。

### 区域截图

1. 用户发起区域截图。
2. 在冻结画面中自由框选或单击窗口，确认选区。
3. 选区保持在冻结桌面上，并在底部横向工具栏中完成标注、马赛克、钉图、OCR、复制或保存。
4. 用户双击选区、按 `Enter` 或点击“完成”后，应用把最终渲染结果写入剪贴板并结束；除非用户显式点击“保存”，否则不写磁盘。

### 滚动截图

1. 用户先切换到目标窗口，再发起滚动截图。
2. 应用按触发瞬间鼠标直接命中结果预选最近的 AX 滚动容器；鼠标位于目标窗口非滚动区域时预选整窗。遮罩内只在该已验证区域与整窗之间切换，用户一旦开始拖动、移动或缩放即进入自定义选区模式。
3. 最终选区绑定到稳定窗口且 Post Event 已授权时可自动滚动；手工调整选区不改变自动模式。条件不满足时明确停止并提示，用户可主动切换手动模式。
4. 浮层显示帧数与采集状态，用户可停止并拼接或取消。
5. 应用过滤重复帧、估算重叠并生成长图。
6. 如果匹配失败，用户先将页面复位，再选择减小步长重试、重新框选或切换手动；若已有至少 2 帧可靠前缀，也可先进入编辑器检查并保存已完成部分。重试在上一轮清理完成后重新进入选区。
7. 长图进入编辑器，继续标注、OCR、钉图或保存。

### OCR 截图

1. 用户发起 OCR 截图并选择区域，或从编辑器选择图片。
2. Vision 在本机识别。
3. 结果窗口展示可编辑文本与置信度。
4. 用户复制文本或保存 TXT。

## 4. 权限、签名与隐私

- 只通过 macOS 系统 API 请求屏幕录制和 Post Event 权限，不修改、删除或绕过 TCC 数据库。
- 首次授予屏幕录制权限后必须提示用户完全退出并重新打开应用。
- 自动滚动只在 Post Event 已授权时运行。
- 文件索引、匹配、OCR、二维码识别、截图处理与拼接均在本机执行，不依赖网络服务。
- 主 App 开启 App Sandbox、用户选择目录读写和 app-scoped bookmark 权限；不得使用沙盒例外扫描未授权用户目录，也不得将 SQLite 索引内容上传。
- 稳定的代码签名身份是持续复用屏幕录制授权的前提之一。
- 开发者账号与签名 Team 仅保存在开发者本机，不写入仓库；真实截图与 TCC 持久性仍必须使用稳定签名构建执行实机验收，不能用禁用签名的测试产物代替。
- `CODE_SIGNING_ALLOWED=NO` 构建只用于编译和自动测试，不能替代稳定签名下的真实权限验收。
- Mac App Store 交付必须提供正式 AppIcon、有效分发签名与签名沙盒 Archive；工程已具备 AppIcon，分发签名与 Archive 上传仍需发布前实机验证。

## 5. 非功能要求

- 使用原生 SwiftUI/AppKit 和标准 `.xcodeproj`。
- 不依赖 CocoaPods、Homebrew 或第三方 Swift Package。
- 文件扫描、文件复制/移动、截图采集、OCR 和滚动拼接不得阻塞主界面；任务需支持取消或明确的生命周期收口。
- macOS 14 及以上截图必须使用 ScreenCaptureKit；macOS 13 的旧 Core Graphics 捕获只能存在于独立兼容后端，不能成为新系统的静默降级路径。
- SQLite 打开与迁移不得发生在 App/SwiftUI 主线程构造阶段；搜索查询连接与后台 generation 写入连接必须分离，并通过 WAL 读取已提交快照。
- 索引查询、generation 中止、路径移动/删除和 dirty 标记失败必须显式传播到 UI 或结构化日志，禁止伪装为空索引或静默成功。
- 文件复制/移动进入系统文件操作后不承诺中途取消；即使查找浮窗关闭，操作仍需在后台完成，并通过 SQL 路径更新或 dirty 标记交给下个后台周期收口。
- 查询不得启动或唤醒后台扫描。自动增量不得与查找面板、截图、OCR 或文件操作并行消耗资源；首次和用户主动快速索引允许与查找面板并行，以保证已提交目录可立即查询，但仍不得与截图、OCR 或文件操作并行。
- “CPU 低于 1%”按空闲后台索引期间的 60 秒平均值验收；QoS 或单次瞬时采样不能作为达标证据。
- 全局快捷键注册、权限状态、保存失败和滚动降级必须提供可读反馈。
- 所有设置需在应用重启后保持。
- 滚动拼接必须设置帧数、时长、输出高度和总像素上限。

## 6. 验收标准

- 产品、Xcode Project、Target、Scheme、模块、可执行文件统一命名为 `PEEK`，Bundle ID 为 `com.shawnshoper.peek`。双击 `PEEK.xcodeproj` 可在 Xcode 中打开，选择 `PEEK > My Mac` 后可编译。由于应用身份已变更，首次运行必须重新授权截图、辅助功能和用户目录，旧应用容器数据不作为升级来源。
- 启动后只出现菜单栏图标，不自动打开普通主窗口；设置窗口仅在用户主动选择“设置”时出现。
- 菜单栏可打开文件查找浮窗；应用、文件、文件夹和隐藏项目能被本地索引检出，Spotlight 关闭时仍可工作。
- 空数据库时执行空查询和关键词查询均立即返回空结果，且不创建 staging generation、不新增 dirty path、不读取测试目录。
- 后台完整扫描完成后结果跨应用重启仍可查询；扫描中 staging 数据不可见，提交失败或取消后旧 active generation 仍可查询。
- 新增授权目录会进入下一次后台任务；移除授权后该根立即不再参与查询。未授权目录、失效 bookmark 和其他应用容器不得被越权扫描。
- Finder 或其他应用在已授权目录内产生的变化，在该根的周期轮换完成后进入新提交代；轮换前允许暂时查不到，输入查询不得触发补扫。
- 空闲索引 60 秒进程采样中，索引工作造成的平均单核 CPU 占用低于 1%；打开查找面板或开始截图/OCR/文件操作后，staging 写入停止增长。
- 全拼、拼音首字母、英文首字母和非连续字符查询均有确定结果；“全部”分类拆分为“应用结果”和“文档结果”，应用分组优先，同组内按匹配度排序。
- 查找浮窗支持上下键、`Enter`、`Tab`、`Esc`，Quick Look 与底部八项操作区可见；禁用操作显示原因，复制/移动不覆盖已有目标。
- 默认 `Control + Space` 无冲突时可打开查找；被输入法或系统项占用时准确显示冲突，可自定义并在重启后保持。
- 连续启动两次后，系统中仍只有一个 PEEK 实例。
- 默认 `Option + Command + 2/3/4` 可分别触发区域、滚动和 OCR 截图；四项动作的自定义、停用、恢复默认与重启持久化均有效，菜单栏同步更新。
- PEEK 内部重复、菜单快捷键占用及可读取的 macOS 系统快捷键冲突会被拒绝；Carbon 独占注册失败或系统检查受限时可看到准确原因。
- 首次屏幕录制授权后应用提示重启；重启前不误报截图已可用。
- 区域截图支持自由选区和窗口选取，编辑器可创建、移动、撤销与重做标注和像素化马赛克。
- 截图完成默认只写入系统剪贴板；工具栏显式“保存”可写入用户选择的 PNG 路径，应用不维护截图历史。
- 同一时间可存在多张钉图，关闭单张不影响其他钉图。
- OCR 在断网条件下仍可识别本地图像并输出可复制文本。
- 二维码在断网条件下仍可识别文本、网址和内嵌图片；网址不会自动打开，远程图片地址不会被静默加载。
- 含二维码的普通选区会在工具栏下方显示点击引导；无二维码、检测失败或用户关闭选区时不显示提示，且检测任务可取消。菜单栏下拉菜单始终不显示二维码相关入口或状态。
- 滚动截图可手动停止或取消；重复帧不进入结果，拼接尺寸与重叠数据一致。
- 未授予 Post Event 权限或窗口锚点失效时自动滚动不执行，并明确提示原因，不静默降级。
- 自动滚动实际步长默认不超过选区高度的 35%，受像素设置上限约束且始终不超过选区高度的 45%；小选区不存在 100 像素保底。
- 可恢复的滚动失败提供减小步长、重新框选、切换手动和纯文字诊断；至少 2 帧已通过校验时可编辑并保存可靠前缀，只有首帧时不显示部分保存；重新采集不会与尚未结束的任务并发。
- 自动测试覆盖文件查找匹配/排序/隐藏项目、滚动重叠估算、像素拼接、截图错误提示和全局快捷键配置。
- 使用有效 Apple Development identity 稳定签名并由用户完成系统授权后，才执行真实截图与 TCC 持久性验收。

## 7. 暂不实现

- 录屏与 GIF 录制。
- 云同步、多设备同步与团队协作。
- 滚动截图的内部悬浮元素消除和语义级智能接缝修复。
- 受保护窗口或 DRM 内容捕获。
- 任何网络数据抓取、网页登录或账号凭证管理功能。
